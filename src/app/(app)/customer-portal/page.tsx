import { SectionPage } from "@/components/app-shell/section-page";

export default function CustomerPortalPage() {
  return (
    <SectionPage
      title="Customer Portal"
      description="A customer-facing workspace for upcoming visits, quotes, invoices, service notes, and pool history."
    />
  );
}
